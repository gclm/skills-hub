interface:
  display_name: "Code Review Expert"
  short_description: "Senior engineer code review: SOLID, security, performance, error handling"
  default_prompt: "Review current git changes for SOLID violations, security risks, concurrency issues (data race, TOCTOU, goroutine leaks), performance problems (lock contention, N+1, polling, memory), error handling issues, and boundary condition bugs."

# Agent-agnostic skill - works with any LLM provider.
# No provider-specific configuration required.
