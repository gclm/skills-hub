interface:
  display_name: "Skills Hub"
  short_description: "Repo-specific patterns and workflows for skills-hub"
  default_prompt: "Use the skills-hub repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true